<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:controls="clr-namespace:VideoPlayback.Controls;assembly=VideoPlayback"
             x:Class="Offline9GagDownloader.BrowsePage"
             Title="BrowsePage">
    <Grid x:Name="content">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="10*"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" x:Name="Title" HorizontalOptions="Center" FontSize="Header" FontAttributes="Bold"></Label>

        <HorizontalStackLayout x:Name="MediaStackLayout" HorizontalOptions="Center" Grid.Row="1">
            <controls:Video x:Name="video" IsVisible="false" HorizontalOptions="Center"/>
            <ScrollView x:Name="MediaScrollView" >
                <Image x:Name="image" />
            </ScrollView>
        </HorizontalStackLayout>

        <Button Grid.Row="10" 
                Text="Next"
                HorizontalOptions="Center"
                Clicked="NextPostButtonClicked" />
    </Grid>
</ContentPage>